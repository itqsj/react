import React, { FC } from 'react';

const Loading: FC = () => {
    return (
        <div
          style={{
            width: '100%',
            height: '100%',
            display: 'flex',
            justifyContent: 'center',
            alignItems: 'center',
          }}
        >
          123
        </div>
    );
};

export default Loading;
